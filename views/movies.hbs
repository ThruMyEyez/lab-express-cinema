<h2>Click on the movie to check the showtimes!</h2>
<ul>
  {{#each movies}}
    <li>
      <a href="/movies/{{_id}}">
        <img src="{{image}}" alt="{{title}}" />
        <h4>{{title}}</h4>
      </a>
    </li>
  {{/each}}
</ul>